import React from 'react'

const EventsContainer = ({ name, desc, date, organizer, ticketType }) => {
    return (
        <div className=''>
            <div className="">{name}</div>
        </div>
    )
}

export default EventsContainer